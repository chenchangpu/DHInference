各位老师好，我是 今年 5 月份入职的实习生 陈昌朴，主要负责万相视频生成模型的量化探索。

今天我的答辩主要围绕量化的核心问题展开，就是：**如何在保证视频生成质量的前提下，提高量化的性能，进一步降低视频生成模型的推理成本。**

这段实习期间，我主要做了三方面的工作。

第一部分，是 Sparse Linear Attention，也就是 SLA 的系统性能优化；\
第二部分，是 FP8 Block Sparse Attention，也就是 稀疏Attention 的性能优化和精度修复；\
第三部分，是进一步探索 NVFP4 这种 4bit 量化在视频生成模型 Attention 上的应用。

---

## 一、背景和问题

首先介绍一下业务背景。

以 DiT 为主干的视频生成模型一个显著的特点，就是 输入序列很长，计算量非常大。

在我们的模型里面，Attention 和 Linear，以及 MoE 在内的线性层，占到了整个推理时间的 90%。

所以从业务角度来说，如果希望明显降低推理成本，量化是非常重要的一条路线。

目前业务上已经在尝试低比特量化，例如 Attention 使用 FP8，Linear 和 MoE 使用 NVFP4。

理论上，量化的比特数降低以后，一方面可以减少数据搬运，另一方面可以更多利用低精度 Tensor Core，因此能够带来比较明显的计算收益。

但在实践上，量化在业务场景还存在两个主要难点：

第一个问题是，**量化之后精度会下降。**

比如对 BSA 进行全量的 FP8 量化，会在一些测试样例上出现肉眼可以感知的画质退化，因此没有办法直接上线。

进一步降到 4bit 以后，这个问题会更加严重。

第二个问题是，**量化带来的性能提升达不到预期。**

以 BSA FP8 为例，实际算子测下来 MFU 只有大约 30%，距离硬件的理论能力还有比较明显的差距。

另外，实际部署中还有 量化、mask计算，等数据处理的额外开销，这些开销太大的话，也会影响量化在端到端上的性能收益。

所以我在这段实习期间，基本就是围绕这两个问题展开：

**第一，怎样尽可能消除量化带来的精度损失；第二，怎样提升量化在端到端的性能收益。**

我基于这个目标，主要进行了三个方面探索，下面讲展开讲述。

---

# 二、工作一：SLA 全链路性能优化

第一部分是 SLA，也就是 Sparse Linear Attention 的性能优化。因为业务已经确认会使用 SLA，但是 SLA 相比 dense attention的性能收益不达预期，需要进行系统的优化。

我把 SLA 拆成了两部分：

一部分是 Sparse Attention，也就是其中真正做稀疏计算的部分；

另外一部分，是非 Sparse Attention 的计算部分，也就是相对量化、掩码计算、线性注意力等其他操作。

然后分别统计它们的耗时占比和计算利用率。

分析之后我发现，这两个部分的瓶颈其实完全不一样。

**非 Sparse Attention 部分主要是 Memory Bound；而 Sparse Attention 部分主要是 Compute Bound。**

针对两种不同的负载场景，我是分开进行了优化。

对于 Memory Bound 的部分，我重点解决的是访存效率问题。

一个优化是通过算子融合，减少中间结果的读写，从而减少 global memory 的访存量。

第二个方向是针对具体算进行优化和调优，通过合并访存等技巧，提高带宽利用率。

最终，在 L20Z 上，Memory Bound 部分获得了大约 1.42 倍加速， L20D 上，获得 **1.9 倍加速**。

---

接下来是 Sparse Attention 部分。

这一部分是典型的 Compute Bound，而且它在 SLA 里占据了非常大的计算比例，尤其是序列长度增加以后。

因为在3.0之前的业务上，Attention的量化会使用 INT8 量化，而 INT8 量化相比 FP8 量化 在性能上会有大约 5% 的差距，所以这里我开展的工作在于找到缩小 INT8 量化 和 FP8 量化性能差距的优化方法。

我通过 kernel 的分析 和 一些消融实验，发现在 INT8 量化的瓶颈 ，在 softmax 前的一段 INT8 到 FP32 的转换，也就是 I2F 指令。

进一步分析后，我发现这段转换其实可以利用 IEEE-754 浮点数的表示性质，通过等价变换，把原来的 I2F 转换成整数加法形式。

这样做以后，就可以把原来 I2F 带来的延迟，通过指令级并行隐藏掉。

最终，算子性能的整体提升大约 **2.5%**。

这个优化本身的数字并不是特别大，但我认为它比较有价值的一点是：

**它不是只针对某一个模型或者某一个 kernel 的特例，而是一种可以迁移到其他 INT 量化 Attention 算子中的优化思路。**

---

# 三、工作二：FP8 BSA 性能优化与精度修复

第二部分是 FP8 BSA。

这也是整个实习期间，我认为和业务结合比较紧的一项工作。

因为业务上目前对 BSA 使用的是 FP8量化， 但存在两个比较明确的问题：

第一，**质量上还有 bad case，不能上线；**

第二，**算子性能不够高，MFU 大约只有 30% 左右。**

我主要也是针对这两个问题展开工作。

---

## 3.1 BSA FP8 精度修复

首先是精度问题。

一开始，我先对照 SageAttention2 的量化思路，确认 K smoothing、P scale 等无损的精度优化方法已经在 BSA FP8量化kernel中实现。完成对齐以后，可以确认算子本身的精度是基本可靠的。

接下来，是进行量化敏感性分析。

我逐个分析不同 layer、不同 head 对量化的敏感程度，通过 余弦相似度、L1 相对误差、RMSE 等指标，判断哪些位置对于 FP8 最敏感。

结果发现，量化严重退化的情况，高度集中在非常少的一部分固定 layer/head 上，我进一步在不同 prompt、不同 step 上做了稳定性分析，发现这些敏感 layer/head 的位置基本是稳定的。

最后，我是对浅层的layer 和 敏感的layer 做了精度保护（也就是采用bf16精度）。一方面保护最浅层的layer，避免量化误差在网络前面不断传播和累积；另一方面，对真正敏感layer 做保护，避免单层的量化精度损失过大。

最终，相比整个 BSA 全部使用 FP8 的方案，只增加非常有限的 BF16 计算，就能够把 bad case 明显改善到满足上线质量要求。端到端相比全 FP8 的性能损失大约只有 **1.5%**。

所以我最后也把这一套算子精度检查、敏感性分析和混合精度流程沉淀成了一套 skill，可以方便地复用。

---

## 3.2 BSA FP8 性能优化

精度问题解决以后，我继续分析性能。

当时 BSA kernel 的一个核心问题是：

**理论上的 FP8 Tensor Core 算力并没有被充分利用。**

所以我在 kernel 内部增加了一些可以开关的旁路，通过对照实验，把 kernel 拆开，分别判断不同阶段到底占了多少时间。

最后定位到一个比较关键的问题：

当前 kernel 在 M=64 的情况下使用的 tile 形状，对应的是硬件“半宽”的数据通路，因此 Tensor Core 并没有被充分利用。

所以我尝试重组 tile，把 M 从 64 调整到 128，然后进行算子的调优，发现 M=128 相比 M=64，TFLOPS 大约提升了 **1.2 倍**。

但是 M=128 需要对相邻 Q tile 的 KV tile 进行合并，会增加冗余的计算量，实际算子的加速不可能完全达到1.2倍。

所以我进一步在 Wan 2.1 真实数据分布进行了净收益分析。

发现最终在真实 workload 下，这个 M=128 的方案能够带来大约 **1.05 倍**的性能收益。

所以这部分工作主要是给业务上探索出一个可尝试、可复用的 BSA 算子性能优化的方案。

---

# 四、工作三：NVFP4 低比特量化探索

4bit 相⽐ 8bit 算⼒更⾼，能够带来性能上的收益，但直接使⽤4bit量化会造成较大的精度损失，需要通过算法补偿精度，使4bit量化能达到上线的质量，同时要尽可能减少算法。我首先是 实现了 flash attention fp4的量化算子，如下图所示，确认了相比FP8量化确实是有性能上的提升，相比 FA4 FP8 量化大约有 7.5% 的性能提升（Softmax是瓶颈），这个提升没有想象中那么大，所以进一步要求我们的精度补偿方案需要尽可能小的引入开销。

# Attention NVFP4：为什么传统 smoothing 不适用

接下来是我认为第三项工作里面最核心的一部分：

**Attention 的 NVFP4 量化。**

这里一开始其实遇到了一个比较典型的问题：

业界已经有一些比较成熟的量化方法，比如 SageAttention3 里面的 Q/K smoothing，以及比较经典的 SmoothQuant。

所以最初的思路也是希望直接把这些方法迁移到我们的 Attention NVFP4 上。

但实验结果并不好。

于是我没有继续简单地调参数，而是回到数据分布本身，去分析：

**为什么这些方法在万相模型上失效？**

最后我发现了两个比较关键的现象。

第一个现象是：

我们 Attention 里面的 outlier channel，它们本身的 mean 非常接近 0。

所以传统的 smoothing，比如减去 mean，本身几乎不能降低这些 channel 的最大绝对值。

因此这种方法自然很难解决 outlier。

第二个现象更加关键。

我发现 Q 和 K 中的 outlier channel 位置具有比较高的重合度。

这意味着，如果按照 SmoothQuant 的思路，去压低 Q 的 outlier，同时把 scale 转移到 K，那么实际上很容易把问题从一边转移到另一边。

也就是说：

**Q 和 K 并不是一个存在明显不对称的关系。它们的困难 channel 很大程度上是重合的。**

所以最终我得出的结论是：

**这里真正需要解决的，并不是简单地把 outlier 的数值压下来，而是改变 NVFP4 的分组方式。**

---

# Channel Permute：从“改数值”转向“改分组”

这也是我这部分工作的核心。

NVFP4 是 per-block 的量化方式。

所以一个很关键的问题就是：

**哪些 channel 被分到同一个 block 里面？**

而原来的分组方式是由 channel 的物理顺序直接决定的。

但从数学上来说，如果对 channel 做一个 permutation，满足：

xWᵀ = (xP)(WP)ᵀ，

那么整个计算结果其实是不变的。

这就给了我们一个机会：

**既然重新排列 channel 不会改变模型计算结果，那我就可以通过重新排列 channel，改变每个 quantization block 里面包含哪些 channel，从而降低量化误差。**

于是我设计了 Channel Permute 方法。

具体来说，我以量化误差最小为目标，用 F2 范数对量化误差进行近似，再通过随机搜索寻找更加合适的 channel 排列。

最终得到的 permutation，是针对每个 layer、每个 head 单独离线搜索出来的。

搜索完成以后，这个 permutation 是固定的。

推理阶段可以直接把它固化并融合到 Q/K 的量化算子中，因此基本不产生额外推理开销。

所以这个方法本质上做的事情非常简单：

**不改变模型原来的计算，只改变量化时 channel 的分组方式。**

---

# Channel Permute 的最终效果

接下来介绍最终结果。

在不同 NVFP4 覆盖率下，Sage3 加 Channel Permute 整体都优于单独使用 Sage3，也优于 Sage3 加 Hadamard。

更重要的是，从实际业务角度看，在满足上线质量要求的情况下：

原来 Attention 的 NVFP4 覆盖率大约只有 **20%**，

使用 Channel Permute 之后可以提升到 **50%**。

也就是说，可用覆盖率提升了 **2.5 倍**。

进一步看质量指标：

Attention NVFP4 的平均质量提升大约 **0.97 dB**；

最差样本提升大约 **1.9 dB**；

另外，我还验证了固定的 layer/head permutation 在不同 prompt 上具有比较好的泛化能力。

所以最终可以离线确定并固化下来，推理阶段基本没有额外成本。

从业务角度来说，这一点非常重要。

因为如果一个量化算法虽然能提高精度，但是每次推理都增加大量额外计算，那么它未必有真正的上线价值。

而 Channel Permute 的特点就是：

**把主要搜索成本放在离线，把收益留到在线。**

---

# 五、整体成果总结

最后，我总结一下这段实习期间最主要的结果。

第一部分，SLA 性能优化。

我通过全链路 profiling，把 SLA 拆解成 Memory Bound 和 Compute Bound 两类问题，分别采用访存优化、算子融合以及关键路径 I2F 消除等方式。

其中 Memory Bound 部分在 B300 上实现了大约 **1.9 倍加速**，Sparse Attention 算子性能提升大约 **2.3% 到 3.2%**。

最终在满足上线质量的情况下，整个 DiT 端到端可以达到 **1.16 倍加速**。

这里的 1.16 倍是整个 DiT 的端到端收益，不是单个 Attention kernel 的收益。

---

第二部分，FP8 BSA。

我完成了从精度对齐、layer/head 敏感性分析，到混合精度保护，再到 kernel 性能优化的一整套流程。

通过只保护少量敏感 layer，在满足上线质量的情况下，相比全 FP8 BSA，端到端性能损失大约只有 **1.5%**。

同时通过 M=128 tile 重组，也验证了一套 BSA FP8 的性能优化方案。

---

第三部分，NVFP4。

在 Attention 上，通过分析 Q/K 数据分布，发现传统 smoothing 方法在万相模型上的局限，进一步提出 Channel Permute 方法。

最终使 Attention NVFP4 在满足上线质量的前提下，覆盖率从 **20% 提升到 50%**。

---

# 六、总结与反思

我对视频⽣成模型和当前业务有了更加深刻的认识，深⼊理解并实践了多种量化算法和策略，丰富模型量化的经验。

我提升了对模型性能优化的能⼒，能够更加熟练地运⽤各种⼯具进⾏性能分析与优化。

为了保证量化的精度与性能，需要与算法团队进⾏更加密切的合作交流。&#x20;

---

以上就是我这段实习期间的主要工作。

整体来说，我主要围绕一个目标展开：

**在不牺牲视频生成质量的前提下，通过量化和系统优化降低推理成本。**

最终从 SLA 性能优化、FP8 BSA 精度与性能优化，到 NVFP4 量化算法探索，分别从系统、算子和算法三个层面推进，也形成了一些可以复用的方法和经验。

我的汇报就到这里，谢谢各位老师。
